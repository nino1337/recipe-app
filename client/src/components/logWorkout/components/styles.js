import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {},
  selectRoutine: {
    width: 300,
    marginBottom: 24,
  },
  textField: {
    width: 100,
    '& input': {
      padding: '12px 10px',
    },
  },
  logTable: {
    marginBottom: '8px',
  },
});
