import { makeStyles } from '@material-ui/core/styles';

export default makeStyles({
  root: {},
  selectRoutine: {
    width: 300,
    marginBottom: 24,
  },
  textField: {
    width: '100%',
    maxWidth: 80,
    '& input': {
      padding: '6px',
    },
  },
  logTable: {
    marginBottom: '8px',
  },
});
